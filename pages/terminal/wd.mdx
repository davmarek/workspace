# wd

`wd` is a small CLI tool used to create waypoints in your file system.

You can install it with Fig, which will also help you with the available commands.

## Create waypoint

```bash
wd add [name]
# for example:
wd add api
```

## Navigate to waypoint

```bash
wd [name]
# for example:
wd api
```
