import { Callout } from 'nextra-theme-docs';
import Image from 'next/image';
import exaImg from '/public/assets/exa.png';

# exa

`exa` is a replacement for the default `ls` command for listing directories and files.

It uses **color**, to make the output **more readable**.

<Callout type="info">
  I recommend setting an alias like `ll='exa -l -a -h -s=type --no-user'`
</Callout>

<Image src={exaImg} alt="exa" />
